{
  "name": "codex-discord-presence",
  "version": "2.5.2",
  "private": true,
  "description": "Native Discord Rich Presence for the ChatGPT Codex desktop app on Windows.",
  "license": "MIT",
  "scripts": {
    "check": "node --check src/daemon.js && node --check src/hook.js && node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
